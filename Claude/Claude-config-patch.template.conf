# Claude Quantumult X minimal patch template
# STATUS: TEMPLATE ONLY — do not activate until <USER_SELECTED_FIXED_NODE> is replaced
# PURPOSE: deterministic first-party Claude/Anthropic routing

# ===== [policy] add one static group =====
static=Claude, <USER_SELECTED_FIXED_NODE>

# ===== [filter_remote] insert AFTER Direct.list and BEFORE Advertising.list =====
https://raw.githubusercontent.com/clxmhcs/quantumult-X-fenliu/refs/heads/main/Claude/Claude.list, tag=Claude, force-policy=Claude, update-interval=172800, opt-parser=true, enabled=true

# ===== keep unchanged =====
# DNS / no-ipv6 / udp_whitelist / MITM / Rewrite / Advertising / ChinaMax / Final
# Do not add Claude/Anthropic domains to MITM or Rewrite.
# Do not use an automatic latency/load-balance group as <USER_SELECTED_FIXED_NODE>.
# The selected node must be an exact fixed server tag chosen by the user in a manner consistent with the service provider's current policies.
