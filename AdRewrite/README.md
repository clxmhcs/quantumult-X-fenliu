# 广告拦截目录

本目录作为 App 广告拦截项目的一级总目录。

## 目录规范

每个 App 必须使用独立的二级目录，JS、snippet 及后续相关文件全部放入对应 App 目录，不在 `广告拦截/` 一级目录混放 App 文件。

当前结构：

```text
广告拦截/
├── 百度拦截/
├── 酷狗音乐拦截/
├── 优酷拦截/
├── 爱奇艺拦截/
├── 腾讯视频拦截/
└── 抖音拦截/
```

百度主 App 文件统一使用 `baidu-app-` 前缀。

后续开发其他 App 时，同样只在各自二级目录内新增和维护文件。
